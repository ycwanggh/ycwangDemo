<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.ycwang.ycwangdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewdraghelper.ViewDragHelperActivity" />
        <activity android:name=".recyclerview.RecyclerViewActivity" />
        <activity
            android:name=".tab.TabActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".edittext.SpaceEditTextActivity" />
        <activity android:name=".slidingtray.SlidingTrayActivity" />
        <activity
            android:name=".slidetab.SlideTabActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".imgcrop.ImageCropActivity" />
        <activity android:name=".keyboard.KeyBoardActivity" />
        <activity android:name=".animation.AnimationActivity" />
        <activity android:name=".textutils.TextUtilsCopyActivity" />
        <activity android:name=".data.DataActivity" />
        <activity android:name=".mvp.PresenterActivity" />
        <activity android:name=".niotifcation.NotificationActivity" />
        <activity android:name=".storageutils.StorageActivity" />

        <receiver android:name=".auto_create.NewAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/new_app_widget_info" />
        </receiver>
        <!-- This service is only used on devices with API v17+ -->
        <service
            android:name=".auto_create.MyDaydreamService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <activity
            android:name=".auto_create.ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".auto_create.ui_compoment_fragment.ItemActivity" />
        <activity
            android:name="com.module.home.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name="com.module.home.MyScrollingActivity"
            android:label="@string/title_activity_my_scrolling"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.module.home.LoginActivity"
            android:label="@string/title_activity_login"></activity>
    </application>

</manifest>